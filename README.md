# Kindred AI

Kindred AI is a prototype care companion that coordinates specialised agents for health, safety, memory, family communication, and household support.

## Demo access

Use the local browser-only demo login:

```text
Username: anita
Password: kindred-demo
```

This is a prototype access screen only. It uses browser `sessionStorage`, does not authenticate against the backend, and must not be treated as production security.
